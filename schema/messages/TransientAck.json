{
  "$schema": "../meta/message.json",
  "message": "TransientAck",
  "note": "What a client sends to say a batch of changes arrived, naming which batch. It is sent for a batch whose values the client dropped as well as for one it took.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "requests",
      "command": 168,
      "direction": "clientToServer",
      "reliability": "reliable",
      "fields": [
        {
          "name": "syncId",
          "type": "u32",
          "note": "Which batch is being acknowledged, as the batch named itself."
        }
      ]
    }
  ]
}
