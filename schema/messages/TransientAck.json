{
  "$schema": "../meta/message.json",
  "message": "TransientAck",
  "note": "What a client sends to say it has taken in a batch of changes, naming which batch.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "requests",
      "command": 168,
      "direction": "clientToServer",
      "reliability": "reliable",
      "fields": [
        {
          "name": "syncId",
          "type": "u32",
          "note": "Which batch is being acknowledged, as the batch named itself."
        }
      ]
    }
  ]
}
