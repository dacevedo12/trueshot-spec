{
  "$schema": "../meta/message.json",
  "message": "QueryStatus",
  "note": "What a client sends to ask a server for its state. It carries nothing of its own. The header names no object: it carries zero.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "requests",
      "command": 20,
      "direction": "clientToServer",
      "reliability": "reliable",
      "fields": []
    }
  ]
}
