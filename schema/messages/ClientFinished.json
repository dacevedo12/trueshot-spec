{
  "$schema": "../meta/message.json",
  "message": "ClientFinished",
  "note": "What a client sends when it has finished with the match. It is sent once, at the end. The header names no object: it carries zero.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "requests",
      "command": 141,
      "direction": "clientToServer",
      "reliability": "reliable",
      "fields": []
    }
  ]
}
