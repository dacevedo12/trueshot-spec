{
  "$schema": "../meta/message.json",
  "message": "EnterLocalSight",
  "note": "What a server sends when a unit comes into what one client can see.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "events",
      "command": 174,
      "direction": "serverToClient",
      "reliability": "reliable",
      "fields": [
        {
          "name": "carried",
          "type": "bits",
          "size": 2,
          "bits": [
            {
              "name": "size",
              "width": 13,
              "note": "How many bytes the messages below occupy in total. Zero in everything observed. Nothing observed sets the three bits above it, so a plain sixteen bit count reads every payload alike."
            },
            {
              "name": "unknown",
              "width": 3,
              "note": "Purpose undetermined. Clear in everything observed."
            }
          ]
        },
        {
          "name": "messages",
          "type": "array",
          "size": "carried.size",
          "note": "Whole messages of the same family, each behind its own length, that a client reads as though they had arrived on their own. No payload observed has carried any.",
          "items": {
            "type": "struct",
            "struct": "CarriedMessage",
            "name": "message"
          }
        },
        {
          "name": "maxHealth",
          "type": "f32",
          "note": "What the unit's health tops out at."
        },
        {
          "name": "health",
          "type": "f32",
          "note": "What its health stands at."
        }
      ]
    }
  ]
}
