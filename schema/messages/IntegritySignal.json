{
  "$schema": "../meta/message.json",
  "message": "IntegritySignal",
  "note": "What a client sends when it notices something about itself worth reporting. Some sessions carry one and others none, and in those that do it arrives once, beside a view drawn further out than the client allows itself.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "requests",
      "command": 125,
      "direction": "clientToServer",
      "reliability": "reliable",
      "fields": [
        {
          "name": "unknown",
          "type": "bytes",
          "size": 8,
          "note": "Two values standing for what was noticed. Observed as zero,."
        }
      ]
    }
  ]
}
