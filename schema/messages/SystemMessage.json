{
  "$schema": "../meta/message.json",
  "message": "SystemMessage",
  "note": "What a server sends to put a line of its own in a client's chat.",
  "revisions": [
    {
      "from": "4.20.0.315",
      "channel": "events",
      "command": 247,
      "direction": "serverToClient",
      "reliability": "reliable",
      "fields": [
        {
          "name": "source",
          "type": "u32",
          "note": "The unit the line is credited to, or zero where none is."
        },
        {
          "name": "text",
          "type": "string",
          "encoding": "utf8",
          "size": 512,
          "note": "The line itself. Written out in full and padded with zero bytes to the width above."
        }
      ]
    }
  ]
}
